# Ledgr — 스트라이프 랜딩 문법 연구

**가상 브랜드** 개발자 결제 도구. 실제 로고·제품·카피·고객사 미사용(로고월은 전부 가상 회사명).

## 연구한 문법
- **★ 스티키 코드 스크롤 동기(스트라이프 시그니처)**: 왼쪽 설명을 읽어 내려가면 오른쪽 코드 패널이 **단계를 따라 전환**(결제 생성 → 웹훅 검증 → 정산 조회 → 환불) + 활성 단계 강조.
- **개발자에게 파는 디자인**: 마케팅 카피 옆에 **실제 코드 블록**을 나란히 — 코드와 마케팅의 공존.
- **그라디언트 메시 히어로**: 흐릿한 다색 그라디언트가 천천히 움직이는 배경 + **섹션 스크롤 리빌**.
- **로고월 + 문서형 푸터**: 신뢰의 로고월, 제품/개발자/회사로 촘촘히 나뉜 푸터.

## 어떻게 재현했나
- 그라디언트 메시 = **CSS `radial-gradient` 다중 + blur + keyframes**(`prefers-reduced-motion` 대응).
- 스티키 코드 워크스루 = **`position: sticky` 코드 패널 + `IntersectionObserver`(단계 동기) + 언어 탭(JS/Python/Go)**. JS는 탭+IO 둘뿐, 미실행 시 첫 블록 정적 노출(폴백).
- 섹션 리빌 = **CSS scroll-driven(`animation-timeline: view()`)**, `@supports` 가드 + `prefers-reduced-motion` 정지.
- 모바일은 2열→1열, 코드 패널을 상단 sticky로 전환(읽으며 코드가 위에서 바뀜). 색·서체는 자체 인디고·틸 팔레트 + 시스템/모노 폰트(시그니처 미사용).

## 브라우저 / 실행
`index.html` 열기.
